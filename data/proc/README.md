# data/proc

Derived inputs to the pipeline. See `../README.md` for what each file is and
which script produces it. Files here that are not listed in the repository
`.gitignore` allow-list are scratch output from exploratory scripts and are not
tracked.
